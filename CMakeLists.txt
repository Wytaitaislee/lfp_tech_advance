cmake_minimum_required (VERSION 1.16.3)

project(${LFP_PRJ_NAME})

# lfp version number.
set (${LFP_PRJ_NAME}_VERSION_MAJOR 1)
set (${LFP_PRJ_NAME}_VERSION_MINOR 0)

add_executable(${LFP_EXEC_DIR} )

